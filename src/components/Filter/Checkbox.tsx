import { FC } from 'react';
import { FilterWithOptions } from '@gymbeam/services/repository/category/types';

type CheckboxProps = {
    option: FilterWithOptions
};

const Checkbox: FC<CheckboxProps> = () => {
    return <></>;
};

export default Checkbox;